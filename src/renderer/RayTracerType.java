package renderer;

/**
 * Ray tracer types enumeration
 * Defines the available ray tracing algorithms
 */
public enum RayTracerType {
    /**
     * Simple (basic) ray tracer
     */
    SIMPLE,
    /**
     * Ray tracer using regular grid
     */
    GRID
}